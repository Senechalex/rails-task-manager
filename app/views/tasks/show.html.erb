<h2><%= @task.title %></h2>
<h3><%= @task.details %></h3>
<%= link_to "Delete", task_path(@task),
      method: :delete,
      data: { confirm: "Are you sure?" } %>
<%= link_to "Edit", edit_task_path(@task) %>

